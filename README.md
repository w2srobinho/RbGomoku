RbGomoku
===

## Install

### Requirements
- Python 3.4+

### Create environment
1. create virtualenv `$ pyvenv env`
2. run `$ source env/bin/activate`
3. run `$ pip install -r requerements.txt`

### Playing
run `$ python main`
